//Codes for experimental categorized options will be tested here
/*
	The loading button will parse the checkboxes and assign a local boolean variable for each checkboxes
	and execute the associated functions.
	
	functiondisable(){
		//1. if past expiry date, disable dynamic asset checkboxes
		//shipCG, funitureCG, interface, voiceline
	}

	functionloadclick(){
		//1. parse checkboxes
		//2. assign boolean values
		//3. if true execute
	}

	functionbypass(){
		//1. create a confirm box.
		//ref (http://www.w3schools.com/js/tryit.asp?filename=tryjs_confirm)
		//2. if yes, remove the alert box and reenable dynamic asset checkboxes.
	}
	
*/

//to be added...
